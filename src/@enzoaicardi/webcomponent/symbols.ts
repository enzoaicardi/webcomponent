/** @internal */
export class Symbols {
    static readonly raw: unique symbol = Symbol();
    static readonly populate: unique symbol = Symbol();
    static readonly definition: unique symbol = Symbol();
}
